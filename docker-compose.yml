services:
  php:
    image: 'akeneo/pim-php-dev:8.3'
    profiles:
      - 'tools'
    environment:
      APP_ENV: '${APP_ENV:-prod}'
      COMPOSER_HOME: '/var/www/.composer'
      PHP_IDE_CONFIG: 'serverName=pim-docker-cli'
      XDEBUG_MODE: '${XDEBUG_MODE:-off}'
      XDEBUG_CONFIG: 'client_host=172.17.0.1'
      EXPERIMENTAL_TEST_DATABASE: '${EXPERIMENTAL_TEST_DATABASE:-0}'
    volumes:
      - './:/srv/pim'
      - '${HOST_COMPOSER_HOME:-~/.composer}:/var/www/.composer'
    working_dir: '/srv/pim'
    command: 'php'
    user: 'www-data'
    init: true
    networks:
      - 'pim'

  fpm:
    image: 'akeneo/pim-php-dev:8.3'
    environment:
      APP_ENV: '${APP_ENV:-prod}'
      BEHAT_TMPDIR: '/srv/pim/var/cache/tmp'
      BEHAT_SCREENSHOT_PATH: '/srv/pim/var/tests/screenshots'
      PHP_IDE_CONFIG: 'serverName=pim-docker-web'
      XDEBUG_MODE: '${XDEBUG_MODE:-off}'
      XDEBUG_CONFIG: 'client_host=172.17.0.1'
    volumes:
      - './:/srv/pim'
    working_dir: '/srv/pim'
    command: 'php-fpm -F'
    networks:
      - 'pim'

  node:
    image: 'akeneo/node:18'
    profiles:
      - 'tools'
    environment:
      YARN_CACHE_FOLDER: '/home/node/.yarn'
      CYPRESS_CACHE_FOLDER: '/home/node/.cypress'
    volumes:
      - './:/srv/pim'
      - '${HOST_YARN_CACHE_FOLDER:-~/.cache/yarn}:/home/node/.yarn'
      - '${HOST_CYPRESS_CACHE_FOLDER:-~/.cache/Cypress}:/home/node/.cypress'
    working_dir: '/srv/pim'
    networks:
      - 'pim'

  selenium:
    image: 'selenium/standalone-chrome:150.0-20260808'
    volumes:
      - './:/srv/pim:ro'
      - '/dev/shm:/dev/shm'
    ports:
      - '${DOCKER_PORT_SELENIUM:-5910}:5900'
    networks:
      - 'pim'

  httpd:
    image: 'httpd:2.4'
    environment:
      APP_ENV: '${APP_ENV:-prod}'
    depends_on:
      - 'fpm'
    ports:
      - '${DOCKER_PORT_HTTP:-8080}:80'
    volumes:
      - './:/srv/pim:ro'
      - './docker/httpd.conf:/usr/local/apache2/conf/httpd.conf:ro'
      - './docker/akeneo.conf:/usr/local/apache2/conf/vhost.conf:ro'
    networks:
      - 'pim'

  mysql:
    image: 'mysql:8.0.34'
    command: '--log_bin_trust_function_creators=1'
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
      MYSQL_USER: '${APP_DATABASE_USER}'
      MYSQL_DATABASE: '${APP_DATABASE_NAME}'
      MYSQL_PASSWORD: '${APP_DATABASE_PASSWORD}'
    volumes:
      - './docker/initdb.d:/docker-entrypoint-initdb.d:ro'
      # Sans ce volume, la base vit dans la couche du conteneur et disparait
      # au moindre `docker compose down`. Le compose Akeneo d'origine l'omet
      # volontairement (usage CI jetable) ; sur un poste de dev c'est une
      # perte de donnees garantie.
      - 'mysql_data:/var/lib/mysql'
    ports:
      - '${DOCKER_PORT_MYSQL:-33006}:3306'
    networks:
      - 'pim'

  elasticsearch:
    image: 'elastic/elasticsearch:8.17.0'
    environment:
      ES_JAVA_OPTS: '${ES_JAVA_OPTS:--Xms1g -Xmx1g}'
      discovery.type: 'single-node'
      xpack.security.enabled: 'false'
      indices.id_field_data.enabled: 'true'
    volumes:
      - 'elasticsearch_data:/usr/share/elasticsearch/data'
    ports:
      - '${DOCKER_PORT_ELASTICSEARCH:-9210}:9200'
    networks:
      - 'pim'

  object-storage:
    image: 'minio/minio:RELEASE.2025-09-07T16-13-09Z'
    entrypoint: >
      /bin/sh -c "mkdir -p /data/asset /data/catalog /data/jobs /data/archive /data/category /data/catalogsmapping
      && minio server --console-address :9091 /data"
    environment:
      MINIO_ROOT_USER: 'AKENEO_OBJECT_STORAGE_ACCESS_KEY'
      MINIO_ROOT_PASSWORD: 'AKENEO_OBJECT_STORAGE_SECRET_KEY'
    ports:
      - '${DOCKER_PORT_OBJECT_STORAGE:-9090}:9000'
      - '${DOCKER_PORT_OBJECT_STORAGE_CONSOLE:-9091}:9091'
    networks:
      - 'pim'

  pubsub-emulator:
    image: 'google/cloud-sdk:375.0.0-emulators'
    command: 'gcloud --user-output-enabled --log-http beta emulators pubsub start --host-port=0.0.0.0:8085'
    networks:
      - 'pim'

networks:
  pim:

volumes:
  mysql_data:
  elasticsearch_data:
